feat: Add initial project setup with Docker

This commit introduces the initial setup for the project, including a README file with project documentation and a docker-compose.yml file for managing the application services.